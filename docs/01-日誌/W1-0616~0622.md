# W1（2026-06-16 – 06-22）

## 本週主軸

- 從 Final_Project 個人專案轉入共用的 CloudDrive 專案，接手 In-App AI 助理（Agent Harness）。
- 第一件事不是寫功能，而是先建立「能量測助理好壞的尺」——eval harness 第一版。

## 完成項目

### 1. 接手 AI 助理模組

確認負責範圍：FR-18–FR-21（對話操作、計畫確認、skill 生成、skill 管理）、NFR-07（本地模型優先與隱私）、第八章 eval harness。

### 2. eval harness 第一版（E1–E6）

- 生成 M2–M5 案例套件，每級 100 案例、共 400 案例；期中把 M2 重新設計為「真的用滿 5 個查詢工具」的情境（非任意組合）。
- 三種執行模式：`api`（in-process mock，供 CI）、`browser`（Playwright 驅動真 UI）、`exec`（實際執行生成的 skill 並驗證產出內容）。
- LLM judge（考官）可切換 provider（gemma／codex／openai）；分數導向範式——考官給分數＋優缺點，不只回 pass／fail。

### 3. 建立 eval-prompt-log

人類可讀的測試 prompt 索引，記錄預期產出與出過問題的 prompt。

## 待辦

- [x] 盤點 backend 中與 harness 有關的既有程式碼，確認架構現況。（W3 完成）
- [x] 環境建置：把 CloudDrive 在本機完整跑起來（前後端、部署、LLM 串接）。（W2 完成）

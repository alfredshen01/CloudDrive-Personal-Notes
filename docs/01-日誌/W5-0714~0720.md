# W5（2026-07-14 – 07-20）

> 本週進行中，以下記至 7/16。

## 本週主軸

- 依 V0.2 需求書逐條審查 harness 模組：9 項有效發現，並盤點五個驗證層的測試現況。
- 審查過程沉澱出證據分級（L0/L1/L2）方法論。
- 前端依賴修復、npm audit 漏洞處置、個人筆記系統整併。

## 完成項目

### 1. 前端依賴與預覽修復

處理 react-markdown 相關依賴問題，恢復前端 Markdown 預覽。

### 2. npm audit 漏洞處置

- **問題**：npm 的安全掃描（npm audit）回報前端依賴鏈中的 undici（Node.js 的 HTTP 用戶端套件）存在已知漏洞。
- **原因**：undici 是開發期依賴（devDependency）間接引入的套件，不會進入建置後的部署產物，實際暴露面限於開發工具鏈；安全掃描預設不區分開發期／部署期依賴，需自行判讀影響範圍再決定處置強度。
- **解決**：確認其屬開發期依賴、不進部署產物後，以 npm audit fix 更新修補。原則：安全掃描的結果先分流（部署產物 vs 開發工具鏈）再決定處置的急迫性。

### 3. V0.2 需求逐條審查

依「雲端硬碟系統 需求與開發計畫書 V0.2」逐條審查 harness 模組，由兩個獨立 session 各審一輪取聯集，共 9 項有效發現、0 項誤報；同步盤點五個驗證層的測試現況與驗收缺口。完整清單、測試現況與驗證計畫見 [V02審查發現與測試現況](../02-筆記-harness/V02審查發現與測試現況.md)。

- **問題**：第一輪審查完成後，對審查結果本身再查核一輪，發現多個「通過」判定的證據只到「檔案存在／關鍵字搜尋有命中」的層級；補查四次、兩次改寫結論——漏看既有的真模型紀錄、也沒有執行一套只需 6 秒的測試。
- **原因**：宣稱與觀察的層級沒有對齊——看到檔案存在就下「功能通過」的結論，效力超出實際觀察到的層級；審查流程缺少強制的證據分級，讓最弱的證據得以支撐最強的結論。
- **解決**：建立證據分級並強制標注——L0（檔案存在）／L1（讀過內容）／L2（執行觀察過行為）；L0 不得支撐「通過」；產物目錄必掃最新一份；交付前回掃全部「通過」項補齊證據等級。核心原則：**宣稱的效力止於實際觀察過的層級**。

### 4. 個人筆記系統整併

個人筆記統一整併——還原五、六月日誌、回補 W1–W5 週日誌、集中 harness 筆記、建立目標×階段追蹤頁與導覽主頁。

## 測試與驗證

- **審查中執行的測試**：後端測試（排除需要真模型的部分）226 個通過；前端測試 21 個通過（助理相關元件）。
- **真模型證據盤點**（結論詳見 V02 文件§三；FR／NFR 為需求書的功能／非功能需求編號）：FR-19 計畫確認與多輪對話記憶有真模型證據；FR-18 只有部分（4 個案例）；FR-20 技能生成全鏈、FR-21 右鍵執行、NFR-07 外部升級只有假模型與單元測試層級的證據；端到端測試（E2E，模擬真實使用者從瀏覽器操作到後端的完整流程）沒有跑過；真人完整走過需求書情境三也還沒做。驗收證據（全案例的真模型評測報告）目前為零。
- **審查方法本身的驗證**：兩個獨立審查各跑一輪再取聯集——單一審查會有遺漏；聯集後的 9 項發現經逐項覆核，0 項誤報。

## 待釐清

- 9 項發現的修復順序：第 1、3、5 項動到 LLM 路徑，修完需要與修復前的量測基準做 A/B 對照，先後順序待排。
- 發現 6「已儲存的工作流程重跑時不再次確認」——「點重跑即視同同意」是否成立，需與團隊確認並寫入文件。

## 待辦

- [ ] 依 V02 文件§四的驗證計畫執行：跑全部案例的真模型評測並保留成績當對照基準 → 跑端到端測試（至少助理相關部分）→ 真人完整走一遍需求書情境三的生命週期。
- [ ] 逐項修復 9 項發現，每項先寫可重現的測試再修。
- [ ] W1–W5 週日誌逐週校正（進行中）。
